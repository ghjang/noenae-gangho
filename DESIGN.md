# 腦內江湖 설계 율법 (DESIGN.md)

코드 곳곳에 흩어진 설계 원칙과 불변식의 전문(全文). 각 조항 끝의 **[보초]**는
그 원칙이 어디서 강제·검증되는지 — 문서가 늙으면 보초부터 확인하라.
세션 작업 규칙(빌드/검증/컨벤션)은 [CLAUDE.md](CLAUDE.md), 조작법은 [README.md](README.md).

## 1. 세계관 — 먹빛 허공 위의 한지

- 캔버스는 먹빛 허공(`--muk-void`), 쪽지(念)는 한지, 강세는 **인주(印朱) 단 하나**.
  색은 전부 CSS 변수 경유 — 하드코딩 hex 금지. [보초: app.css `:root` 토큰]
- 분류색은 오행 5종(`muk/cheong/dan/hwang/nam`) — 추가/변경 시 `COLORS`(store) ·
  `app.css`(`--c-*` + `.node[data-color]`) · `colorLabel`(두 팩) 3곳 동기.
  [보초: scripts/check-strings.mjs가 colorLabel 커버리지 검사]
- 면 위계: 부유 패널(HUD·미니맵·검색·집중 배지)은 `--panel`+진한 헤어라인+그림자,
  전폭 상단 바만 `--chrome`(다크). 밝은 전폭 바는 천박하다(사용자 결).
- 말투는 무협 톤 기본 + 일반(plain) 팩 — **디자인(색·서체·아이콘)은 톤과 무관하게 공통**,
  말투 차이는 문자열 팩 교체로만. [보초: check-strings 키 동기 검사]

## 2. 데이터 모델 — 작게, 명시적으로

- 문서 포맷 `{ app: 'noenae-gangho', v: 3, nodes, edges }`. 스키마 변경 = `v` 인상 +
  `loadData()` 마이그레이션. 선택 필드(`bw?`, `collapsed?`)는 구버전과 자동 호환.
- `snapshot()`은 **화이트리스트**(노드 `id,x,y,text,color,bw?,collapsed?` / 엣지 `id,a,b`)만
  영속 — 실측 `w/h`, 선택, 집중 등 세션 상태는 절대 포맷에 새지 않는다.
- 가져온 JSON은 **검역**: 무명/중복 id 쪽지 폐기, 緣은 양끝 실존·자기 緣 금지·쌍당 한 가닥
  규칙으로 거른다. 실측값 위조(w/h 주입)도 차단. [보초: store `loadData()`]

## 3. 緣(연결)의 율법

- **緣에는 방향이 있다**: `a`=부모, `b`=자식. Tab 가지치기와 비급.md 트리 출력이 이 방향에
  의존하고, 화면엔 화살촉. 방향 전환은 F(뒤집기) — 새 가닥을 긋는 게 아니다.
- **같은 두 쪽지 사이엔 방향 불문 한 가닥** (A⇄B 두 가닥 금지): 중복 판정은 무방향.
  이유 — 쌍방향 두 가닥은 트리 출력을 쌍둥이 순환으로 꼬이게 하고, 기하상 두 곡선이
  정확히 겹쳐 분간 불가. [보초: store `addEdge`/`loadData` · scripts/e2e/edges.cjs]
- 자기 자신과의 緣 금지. **3쪽지 이상 긴 순환은 허용** — 대신 순환을 다루는 모든 연산
  (봉문·정돈·비급 출력·이웃 탐색)에 순환 가드. [보초: scripts/check-graph.mjs 시나리오]
- 봉문(접기) 규칙: ①이미 숨은 뿌리의 봉문은 무효 ②봉문은 자기 조상(순환 동료 포함)을
  숨기지도 건너가지도 않는다 — 펼치기 단추 증발(자기 잠금) 방지. [보초: check-graph 1~5]
- 緣 기하: **양끝 다 변 중앙 앵커**(중심 출발 금지 — 드래그 강조에서 관통선이 들통난다),
  우세 축 판정을 양끝이 공유해 축이 늘 짝 맞는다. 노드 실측 전 폴백(180×48)은
  `nodeBox()` 한 곳에만. [보초: scripts/e2e/anchor.cjs]

## 4. 역사(undo) — 사용자의 시간을 보호하라

- 모든 데이터 변이는 직전 스냅샷을 역사에 쌓고 시작(`markUndo()`), 끝에 저장 예약
  (`scheduleSave()`). 스토어 밖 직접 변이(드래그의 `n.x/y` 등)는 호출부가 둘 다 책임.
- 연속 제스처(타이핑·드래그·넛지·리사이즈)는 같은 key의 0.8초 창 안에서 **한 걸음으로 병합**.
- 복합 변이(가지치기·드롭 연결·가져오기·무리 작업)는 `asOneStep`으로 **undo 1회** 분량.
  **끝났는데 무변이면 역사 원상복구** — 유령 걸음·redo 증발 금지. [보초: e2e/edges.cjs]
- ui 상태(선택·뷰·톤·집중)는 역사 밖. 문서 전환은 역사를 가른다(스택 리셋).

## 5. 저장 — 어댑터 뒤에, 문서 단위로

- 본문 입출구는 어댑터 하나: `load(docId)/save(docId, data)`. localStorage는 기본값일 뿐 —
  VSCode 웹뷰 이식 시 postMessage 어댑터로 교체(파일 하나=문서 하나). 문서 인덱스는
  `persistDocs()`가 같은 항구. [보초: store 상단 주석 · CLAUDE.md 설계 결정 1]
- 저장은 500ms 디바운스 + 떠날 때(pagehide·문서 전환) 즉시 flush. **디바운스 유작이
  다음 문서에 적히는 사고 금지** — 전환은 flush 선행, 삭제는 타이머 무장해제.
  [보초: e2e/docs.cjs]
- 레거시 단일 키(`noenae-gangho-v1`)는 첫 실행에 '강호 1'로 이주 후 **화석 보존**
  (지우지도, 다시 읽지도 않는다). 인덱스가 유실/오염돼도 본문 키(`-doc-*`)를 스캔해
  서가를 재건 — 문서는 잃지 않는다. [보초: e2e/docs.cjs B·C]
- 취향은 따로: 톤은 전역 키, 뷰(팬/줌)는 문서별 키 — 둘 다 snapshot 밖.

## 6. 좌표와 기하

- 노드 x/y는 world 좌표, 화면은 `ui.pan`/`ui.scale` 변환. 새 인터랙션은 `toWorld()` 경유.
- 노드 w/h는 실측(`bind:offsetWidth/Height` — 테두리 포함). clientWidth로 바꾸면 좌측
  오행 띠만큼 앵커가 어긋난다. 언두/불러오기는 직전 실측을 물려받는다(허공 찌르기 방지).
- 줌 한계는 `SCALE_MIN/MAX` + `clampScale` 한 곳 — 하한을 올리면 全(전체 보기)이 큰
  강호에서 잘린다.

## 7. 선택 모델 — 무리와 앵커

- `ui.selectedIds`가 무리(다중 선택), `ui.selectedId`는 **앵커**(항상 무리 안) —
  단일 표적 작업(Tab/F2/Z/Alt+화살표/Shift+2)은 앵커 기준. 緣 선택과 상호 배타.
- 선택 변경은 선택 API(`selectNode/selectEdge/clearSelection/addToSelection/selectMany/
  pruneSelection`)만 경유 — 직접 대입 금지. [보초: CLAUDE.md 규칙 · e2e/multi.cjs]
- 무리 멤버 맨클릭(이동 없음)은 단독 수렴, 잡고 끌면 무리째(Figma 관행). 빠진 앵커는
  마지막 멤버가 승계.

## 8. 보기(View) 상태 — 데이터가 아니다

- 집중(`focusId/focusDepth`)·문서 전환 플래그 등은 **세션 전용**: snapshot/undo 밖.
  뷰 모드(`viewMode`)는 톤/뷰포트처럼 취향 — 문서별 키로 저장하되 역시 snapshot 밖.
- 최종 숨김 = 봉문(computeHidden) ∪ (집중 이웃 밖) — **파생 하나로 합성**해 全 맞춤·전도·
  항법·선택 정리가 전부 공짜로 따라온다. 숨김을 따로 계산하는 코드를 만들지 말 것.
- **뷰는 표현일 뿐, 데이터는 하나** (#42): 어느 뷰에서 바꾸든 모든 뷰에 즉시 반영되고,
  **undo는 뷰 불문 전역**(데이터의 역사지 뷰의 역사가 아니다). 봉문은 모든 뷰가 존중,
  **집중(L)은 캔버스 전용 렌즈** — 오행진 진입 시 해제(켠 채 들어가면 선택 정리가
  버블 밖 카드를 증발시킨다). 선택은 뷰를 넘어 공유(카드 링 ↔ 쪽지 링).
- 문서 전환은 **동기식 절단**: 전환 중 트랜지션 0ms — 두 강호가 겹쳐 보이면 주화입마.

## 9. UI 관행

- 다이얼로그: 주 동작이 오른쪽 끝(웹/맥 관행), 취소/닫기는 그 왼쪽. 제목/버튼 고정 +
  본문만 스크롤. 위험 동작은 확인 카드 — **네이티브 confirm/alert/팝업 금지**(VSCode
  웹뷰에서 차단되는 로드맵 지뢰).
- Esc는 단계식: 열린 것(시트→집중→검색→편집 등)을 먼저 닫고, 마지막에 선택 해제.
- **점프는 데려가기지 펼치기가 아니다**: 검색/오행진 점프는 접힌 '조상'만 열어 대상을
  보이게 하고, 대상 자신의 봉문(접어둔 의지)은 보존한다. [보초: e2e/board.cjs]
- **베기는 보이는 것을 벤다**: 접힌 쪽지를 베면 그 봉문이 숨기던 후손도 동반 — 닫힌
  상자의 내용물이 쏟아지는 놀람 방지. 다른 경로로 보이던 쪽지는 살아남는다(다중 부모
  안전 — computeHidden 그늘 차집합). 펼친 마디는 마디만. [보초: e2e/edges.cjs]
- 캔버스는 **팬 우선**(맨 드래그=팬, 올가미는 Shift) — 디자인 툴 주류와 다른 의도된 절충,
  손버릇 보호. Shift 누름은 조준 레티클 커서로 예고.
- 상단 바 배열 서사: **문서 → 창작 → 입출력 → 위험 → 메타** (왼→오). 버튼 등고선 31px.
- 모바일(≤640px)은 뷰잉용 최소 대응(바 2단, 전도/낙관 숨김, 줌 위젯 중앙) — 편집 지원은
  #88의 별도 공사.

## 10. 커서 — OS를 믿지 않는다

- 앱 안에서 OS 커서 완전 퇴출: 1비트 XOR 계열(I빔·십자선·리사이즈)이 하드웨어 커서
  플레인에서 '보더 없는 흰 판'으로 깨지는 고질병 우회(#75). 전부 커스텀 data URI.
- 글리프는 Phosphor Icons 듀오톤(MIT) 개작 + 자체 획 글리프 2종. **클릭 핫스팟은 글리프
  기하에서 계산해 명세**(화살표 촉 3,3 · 손가락 끝 11,3 · 중심 12,12) — 바꾸면 클릭이
  엉뚱한 데 떨어진다. 조준 레티클만 역상(먹 테+한지 심) — 주 무대가 어두운 허공이라.
  [보초: app.css `:root` 주석 · e2e/cursors.cjs]

## 11. 문구 — 팩이 진실이다

- 화면 문구는 전부 `strings.js` 두 팩(`muhyeop`/`plain`) — App 하드코딩 금지, 새 문구는
  두 팩 같은 키로. 팩은 순수 데이터(함수 금지), 매개변수는 `{x}` + `fmt()`.
- 도움말은 3층: 퀵 카드(`helpQuick` ⊆ helpItems) / 전체 시트(`helpSections` — 전수 분배:
  합집합=전체·중복 0) / README 조작 요결. 조작을 바꾸면 셋 다.
  [보초: check-strings — 고아 키·부분집합·전수 분배 전부 빌드 게이트]

## 12. 의존과 검증

- **런타임 의존성 0** — svelte/vite 외 금지. 필요한 건 자작(신택스 하이라이트)하거나
  인라인(아이콘 SVG — 출처·라이선스를 주석에 명기: Phosphor=MIT, Markdown 마크=PD).
- 자동 게이트는 `npm run check` 3종(문구/봉문/비급 역해석)뿐 — 시나리오 검사가 곧 명세.
  **규칙을 바꾸면 시나리오를 같이 바꾼다.** 렌더링/조작은 `scripts/e2e/` 11종(수동)이 보초.
- 순수층(`geometry/graph/markdown/highlight`)은 DOM·스토어 무관 — 맨 node로 검증 가능해야
  한다. 스토어 import 금지(runes는 맨 node가 못 읽는다).
